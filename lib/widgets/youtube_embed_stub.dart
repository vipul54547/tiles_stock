import 'package:flutter/widgets.dart';

/// Non-web stub. The mobile build plays via youtube_player_iframe instead, so
/// this is never rendered there.
Widget buildYoutubeEmbed(String videoId) => const SizedBox.shrink();
